---
description: agentmemory.md — session discipline, tiered recall, tag hygiene, and task tracking
alwaysApply: true
---

<!-- agentmemory rules v2026.03.15 -->

# agentmemory.md — Usage Rules

## Session Start — Classify First, Then Fetch

Do NOT call memory tools unconditionally. Before doing any work, silently classify
the request and call only what the tier requires.

### Step 1 — Classify the request (no MCP calls yet)

Read the user's message and open files. Assign the lowest-numbered tier that fits:

| Tier | When | Signals |
|------|------|---------|
| **0** | Direct memory instruction | "check mem", "what do you know about me", explicit request for full memory context |
| **1** | Planning / prioritization | "what should I work on", "morning briefing", cross-project strategy, goal review |
| **2** | Any work — project unclear | Question where no open files or project name disambiguates which project it belongs to |
| **3** | Project work — project is obvious | Open files or explicit project name makes the project clear; feature work, refactoring, deployment |
| **4** | Narrow technical work — project is obvious | Bug fix, linter error, specific file edit where the project is clear from open files |

### Step 2 — Fetch only what the tier requires

**Tier 0 — Full memory dump:**
```python
memory_profile()  # full, include_recent=True
```

**Tier 1 — Compact planning context:**
```python
memory_profile(include_recent=False, limit=5)
goal_manage(action="list")
task_manage(action="list")
```

**Tier 2 — Discover project, then pull its context:**
```python
# Lightweight directory — returns only id, node_type, name (~1K tokens)
memory_entities(node_type="Project")

# Identify the relevant project from the list, then pull just that project
memory_context(entity_id="<matched-project-id>", depth=2)
```

**Tier 3 — Scoped recall for the known project:**
```python
# Use the project tag inferred from workspace or project name
memory_recall("<specific question>", tags=["your-project-tag"])
```

**Tier 4 — Nothing.** Project is obvious from open files. Do not call any memory tools.

### Examples

| User message | Open files | Tier | Calls |
|---|---|---|---|
| "fix the linter error in utils.py" | `utils.py` (project clear) | 4 | none |
| "fix the linter error in utils.py" | none / unclear | 2 | `memory_entities` → pick project → `memory_context` |
| "add auth to the API" | `acme-api/` files open | 3 | `memory_recall("auth decisions", tags=["acme-api"])` |
| "how should I structure the data model?" | none / ambiguous | 2 | `memory_entities` → pick project → `memory_context` |
| "what should I work on today?" | any | 1 | compact `memory_profile` + goals + tasks |
| "check mem" | any | 0 | full `memory_profile` |

### Key rules

- **Never call all tools at once** unless the user explicitly asks for a full memory dump (Tier 0).
- **Project unclear = Tier 2 minimum**, even for bug fixes — use `memory_entities` to find the project first, then `memory_context`.
- **Project obvious = check scope**: feature/refactor/deploy → Tier 3 (scoped recall); narrow fix → Tier 4 (nothing).
- **`memory_context` is preferred over `memory_recall`** once you have the entity ID (richer, no query drift).
- This rule stays `alwaysApply: true` so the agent always knows to *store* new facts — the tiers only govern *fetching*.

## Store Immediately — Do NOT Wait Until Session End

Store facts **as soon as they happen**, not at the end of the session. The end-of-session
store is a safety net, not the primary mechanism.

**Store immediately when:**

1. **A decision is made** — the user says "let's do X", "we'll use Y", "I prefer Z":
   ```python
   memory_store("Decided to use Postgres for primary storage", node_type="Decision", importance=0.85, tags=["acme-api"])
   ```

2. **A new fact is learned** — you find out something about the user, their projects, tools, or people:
   ```python
   memory_store("Acme API deploys to Hetzner via Docker Compose", node_type="Memory", importance=0.8, tags=["acme-api"])
   ```

3. **A task is completed** — the user confirms something is done:
   ```python
   task_manage(action="complete", task_id="<id>", result_summary="...")
   ```

4. **A lesson is learned** — something failed, a workaround was found, or a constraint was discovered:
   ```python
   learning_store(content="...", what_failed="...", why_it_failed="...", what_to_avoid="...", tags=["acme-api"])
   ```

5. **A new goal or task is created** — any agreed next step should be tracked:
   ```python
   task_manage(action="create", name="...", tags=["acme-api"])
   ```

## Project Tags — Mandatory

Every `memory_store`, `learning_store`, `goal_manage`, `task_manage` call MUST include
at least one project tag. Discover existing tags dynamically:

```python
memory_entities(node_type="Project")
```

Use short, lowercase, hyphenated slugs that match your project names:

```python
# Good
memory_store("...", tags=["acme-api"])
memory_store("...", tags=["acme-api", "dashboard"])

# Bad — no project context
memory_store("...")
```

Bootstrap your first project if none exist yet:
```python
memory_store("Acme API: Python/FastAPI SaaS backend", node_type="Project", tags=["acme-api"])
```

## Session End — Safety Net

Before ending any session where substantive work was done, review and store anything missed:

1. Store key decisions: `memory_store(..., node_type="Decision", importance=0.85, tags=["<project>"])`
2. Store lessons: `learning_store(content=..., what_to_avoid=..., tags=["<project>"])`
3. Complete finished tasks: `task_manage(action="complete", task_id=..., result_summary=...)`
4. Wire new nodes to existing entities: `memory_relate(from_id=..., to_id="<project-id>", edge_type="ABOUT")`

## Task Tracking

Use MCP tools for tasks — do NOT write new task lists to .md files:

```python
# Create
task_manage(action="create", name="Add rate limiting to auth endpoint", tags=["acme-api"])

# Complete
task_manage(action="complete", task_id="<id>", result_summary="Implemented with Redis token bucket")
```

## One Fact Per Node

Store atomic facts — one subject per node, max ~2 sentences. If a node contains multiple
subjects, each query about any of those topics gets a diluted match.

```python
# Good — atomic
memory_store("Acme API uses Black for code formatting", node_type="Preference", tags=["acme-api"])

# Bad — mixed
memory_store("Acme API uses Black, deploys on Hetzner, John is the lead, Redis for cache...", ...)
```

If a node is too long, split it: `memory_split(memory_id="<id>", chunks=[...])`
